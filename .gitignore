# Node
node_modules/
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*.swo

# Output
output/
dist/
build/

# Note: distribution ZIPs (markdown-c4c-v*.zip) and PDFs are NOT ignored —
# they are committed to the repo so users can download via:
#   - Raw GitHub URL (non-developers)
#   - git clone (developers)
#   - GitHub Releases (also published as release assets)

# Backup
*.bak
*.tmp
*~
